{
  "name": "phonebook_backend",
  "version": "1.0.0",
  "description": "A simple phonebook to restore phone numbers of persons with CRUD operations",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "\"\"",
    "build:ui": "rm -rf dist && cd ../Frontend && npm run build && cp -r dist ../Backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "repository": {
    "type": "git",
    "url": "\"\""
  },
  "keywords": [
    "\"\""
  ],
  "author": "Shravani Bayani",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.2",
    "morgan": "^1.10.0"
  }
}
